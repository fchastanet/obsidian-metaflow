name: Unit test

permissions:
  # used by ouzi-dev/commit-status-updater
  statuses: write

on:
  workflow_dispatch:
  push:

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Sets lint status to pending status
        uses: ouzi-dev/commit-status-updater@v2
        with:
          name: "lint"

      - name: Sets unit-test status to pending status
        uses: ouzi-dev/commit-status-updater@v2
        with:
          name: "unit-test"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        if: ${{ hashFiles('package-lock.json') != '' }}
        run: npm install

      - name: Lint the plugin
        run: npm run lint

      - name: Build the plugin
        run: npm run build

      - name: Test the plugin
        run: npm run test

      - if: always()
        uses: ouzi-dev/commit-status-updater@v2
        with:
          name: "lint"
          status: "${{ job.status }}"

      - if: always()
        uses: ouzi-dev/commit-status-updater@v2
        with:
          name: "unit-test"
          status: "${{ job.status }}"
